#include <stdio.h>
/**
 * main - another function
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("%zu","size of a char:", sizeof(char));
printf("%zu","size of an int:", sizeof(int));
printf("%zu","size of a long int:", sizeof(long int));
printf("%zu","size of a long long int:",sizeof(long long int));
printf("%zu","size of a float:", sizeof(float));
return (0);
}
