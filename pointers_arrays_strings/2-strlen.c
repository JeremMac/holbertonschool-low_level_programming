#include "main.h"
/**
 * _strlen - returns the lenght of a string
 * Return: return the number of characters found
 * char - type of *s
 * @s: member of _strlen
 */
int _strlen(char *s)
{
	int my_tab[0];
	int *cnt;

	cnt = 0;

	while (my_tab[cnt] <= s)
	{ 
		my_tab[cnt] + 1;
	}
	return (cnt);
}
