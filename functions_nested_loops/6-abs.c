#include <stdio.h>
#include "main.h"
/**
 * _abs- prints the absolute value of int
 * Return: Always 0 (Success)
 * int - parameter of _abs
 * @n: member of _abs
 */
int _abs(int n)
{
if (n < 0)
{
n = (-1) * n;
return (n);
}
else
{
return (n);
}
}
