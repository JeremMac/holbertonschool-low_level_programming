#include <stdio.h>
#include "main.h"
/**
 * add - add two int together
 * Return: return the addition value
 * @a: first member of add
 * @b: second member of add
 * int - type of a and b
 */
int add(int b, int c)
{
int f;
f = b + c;
return (f);
}
