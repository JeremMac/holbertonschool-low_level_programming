#include <stdio.h>
#include "main.h"
/**
 * add - add two int together
 * Return: return the addition value
 * @b: first member of add
 * @c: second member of add
 * int - type of a and b
 */
int add(int b, int c)
{
int f;
f = b + c;
_putchar(f + '0');
return (f);
}
