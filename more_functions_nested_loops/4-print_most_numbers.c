nclude <stdio.h>
/**
 *  *main - the program is in here
 *   *Return: Always 0 (Success)
 *    */
int main(void)
{
	int al;
	for (al = 0; al <= 9; al++)
	{
		if (al != 2 && al != 2)
		{
			_putchar(al + '0');
		}
	}
	_putchar('\n');
	return (0);
}
