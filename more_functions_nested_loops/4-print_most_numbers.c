nclude <stdio.h>
/**
 *  *main - the program is in here
 *   *Return: Always 0 (Success)
 *    */
int main(void)
{
	int num;
	for (num = 0; num <= 9; num++)
	{
		if (num != 2 && num != 2)
		{
			_putchar(num + '0');
		}
	}
	_putchar('\n');
	return (0);
}
