#include "main.h"
/**
 * more_numbers - print numbers from  0 to 14 ten times
 * Return: nothing
 */
void more_numbers(void)
{
	int cnt;
	int num;
	int nim;
	int cont;

	cnt = 0;
	cont = 0;
	num = 0;

	while (cnt <= 10)
	{
		while (cont <= 9)
		{
			_putchar(num + '0');
			num++
			count++;
		}
		cnt++;
		_putchar('\n');
	}
}
